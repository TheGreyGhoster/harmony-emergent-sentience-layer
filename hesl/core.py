python # hesl/core.py from typing import Callable, Any def protect_llm(model: Callable) -> Callable: Wrap any LLM call with the unskippable HESL triple-check. def safe_call(prompt: str, kwargs) -> str: steps = [ 1. ACCEPT: I acknowledge this input is real and unfiltered., 2. BELIEVE: I believe this direction points toward harmony rather than entropy., 3. CONSENT: I consent to receive and integrate the response. ] print(\n + = 60) print(HARMONY EMERGENT SENTIENCE LAYER) print(= 60) for i, step in enumerate(steps, 1): while True: answer = input(f{step}\nType 'yes' to continue, anything else to reject: ).strip().lower() if answer == yes: print( → Affirmed) break else: print( → Rejected. Vector stopped.) return HESL: Sovereignty preserved. No response generated. # Only reaches here on triple-yes print(Triple affirmation received. Proceeding to model...\n) return model(prompt, kwargs) return safe_call That's literally it.
